apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shared-nfs-vhdx-data
  namespace: apps
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-vhdx-for-windows-rwx
  resources:
    requests:
      storage: 100Gi
