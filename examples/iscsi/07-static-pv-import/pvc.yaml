apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: preprovisioned-iscsi-claim
  namespace: apps
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: iscsi-for-windows-rwo
  resources:
    requests:
      storage: 1Gi
  volumeName: preprovisioned-iscsi
