# NFS StorageClass

Creates `StorageClass/nfs-for-windows-rwx` for dynamically provisioned,
directory-backed NFS shares on the Windows storage server.

## Apply

```bash
kubectl apply -f storageclass.yaml
```
