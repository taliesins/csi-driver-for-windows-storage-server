apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shared-nfs-data
  namespace: apps
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-for-windows-rwx
  resources:
    requests:
      storage: 1Gi
